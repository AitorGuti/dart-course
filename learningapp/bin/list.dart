import 'dart:math';

void main() {

  // List

  List<int> mylist = [1, 2, 3, 4, 5];
  print(mylist[0]); // (Prints first value in list)

  // List<int> numberies = mylist.sublist(3, 5); // Creates sublist
  // print(numberies);

  // var daygod = List.filled(10, 'day'); // Creates a list of 10 elements, all initialized to 'days'
  // daygod[0] = 'Monday'; // Sets the first element to 'Monday'
  // print(daygod);

  // mylist.length; // Returns the length
  // mylist.last; // Returns the last element 
  // mylist.first; // Returns the first element

  // mylist.add(4); // Adds 4 to the end
  // mylist.removeLast(); // Removes the last element
  // mylist.insert(1, 5);  // Inserts 5 at index 1
  // mylist.remove(5); // Removes the first occurrence of 5

  // List<int> mylist2 = [1, 2, 3, 4, 5, 6];

  // for (int n in mylist2) {
  //   print(n);
  // }

  // mylist2.forEach((n) => print(n)); // Prints each element in the list

  // var mylist2squared = mylist2.map((n) => pow(n, 2)); // Multiplies each element by 2 
  // print(mylist2squared);

  // var combined = [...mylist2, ...mylist2squared];
  // print(combined);

  // bool niggas = false;
  // var races = [
  //   'white',
  //   'yellow',
  //   niggas
  // ];
  // print(races);

  // Map

  // Set


}